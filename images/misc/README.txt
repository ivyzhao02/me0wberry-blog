Images in this folder are shared by non-gallery-specific post categories, such
as beauty, games, and lately. Some files may also be used as stable external
image URLs, so do not delete apparently unused misc images without checking
first.
