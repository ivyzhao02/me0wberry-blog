Some files in this folder may be intentionally kept even when the website does
not reference them directly. Ivy uses a few stable GitHub-hosted image URLs here
for custom Discord rich presence artwork, so do not delete apparently unused
misc images without checking first.
